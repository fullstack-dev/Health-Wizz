import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RenameProviderPage } from './rename-provider';

@NgModule({
  declarations: [
    RenameProviderPage,
  ],
  imports: [
    IonicPageModule.forChild(RenameProviderPage),
  ],
})
export class RenameProviderPageModule {}
